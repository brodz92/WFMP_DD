
<Window x:Class="Ddu.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Ddu.Wpf.ViewModels"
        mc:Ignorable="d"
        Title="FirstStrike DDU (WPF, .NET 8)" Height="720" Width="1100">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Content="Open CSV…" Command="{Binding OpenCsvCommand}" Margin="2"/>
                <Button Content="Save CSV…" Command="{Binding SaveCsvCommand}" Margin="2"/>
                <Separator/>
                <Button Content="Import to WFD" Command="{Binding ImportCommand}" Margin="2"/>
                <Separator/>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Margin="8,0,4,0" VerticalAlignment="Center" Text="Base URL:"/>
                    <TextBox Width="200" Text="{Binding BaseUrl, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Margin="8,0,4,0" VerticalAlignment="Center" Text="Tenant:"/>
                    <TextBox Width="120" Text="{Binding Tenant}"/>
                </StackPanel>
            </ToolBar>
        </ToolBarTray>
        <Grid>
            <DataGrid ItemsSource="{Binding Rows}" AutoGenerateColumns="True" IsReadOnly="False" CanUserAddRows="True"
                      EnableRowVirtualization="True" EnableColumnVirtualization="True"/>
        </Grid>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem Content="{Binding Status}" />
            <StatusBarItem Content="{Binding ProgressText}" HorizontalAlignment="Right"/>
        </StatusBar>
    </DockPanel>
</Window>
